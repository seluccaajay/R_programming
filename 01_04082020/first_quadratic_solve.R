# Let us solve the quadratic equation x^2+x-1=0

# we know that to solve a quadratic equation let us give separate variables

a <- 1
b <- 1
c <- -1

# solving the quadratic variables

(-b + sqrt(b^2 - 4*a*c))/(2*a)
(-b - sqrt(b^2 - 4*a*c))/(2*a)